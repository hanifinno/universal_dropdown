name: super_search_delegate_example
description: Example for super_search_delegate package

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  dio: ^5.9.0
  flutter:
    sdk: flutter
  # http: ^1.5.0
  super_search_delegate:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true